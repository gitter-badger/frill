import Auth from './Auth';
import Example from './Example';

/**
 * List of all actions to use
 */
const Actions = {
  Auth: Auth,
  Example: Example,
};

/**
 * Export all actions
 */
export default Actions;
